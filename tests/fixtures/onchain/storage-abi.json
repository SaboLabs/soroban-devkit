{
  "source_wasm": "crates/sdkt-cli/tests/fixtures/us_new.wasm",
  "expected_abi_fields": {
    "functions_contain": "mint",
    "custom_types_contain": "Circle",
    "events_contain": "Mint"
  },
  "note": "Deterministic, offline-provable assertion: the M44 on-chain ABI path (M41 retrieval -> parse_contract_spec) feeds exactly this ContractSpec shape to the storage analyzer, which surfaces functions/custom_types/events in `storage check`/`analyze` output. CI validates the source WASM declares these ABI fields via offline `sdkt wasm inspect` and that the deployed-spec decode yields the expected names."
}
