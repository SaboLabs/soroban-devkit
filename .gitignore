# Binary
/target
**/*.rs.bk
**/target/

# Stray compiled artifacts
/gen_keys

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Environment
.env
.sdkt.toml.local
*.local

# Cargo registry cache (optional, speeds up CI)
# NOTE: Cargo.lock is intentionally tracked (not ignored) — the CI Action
# installs with `cargo install --locked`, which requires it in the tree.
.hermes/
